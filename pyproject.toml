[project]
name = "neuralsignal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch",
    "transformers",
    "bitsandbytes",
    "datasets",
    "huggingface-hub",
    "pymongo",
    "pandas",
    "scikit-learn",
    "xgboost",
    "hyperopt",
    "mlflow",
    "pygments",
    "pyyaml",
    "tqdm",
    "pytest",
]
